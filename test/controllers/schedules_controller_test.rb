require 'test_helper'

class SchedulesControllerTest < ActionDispatch::IntegrationTest
  # setup do
  #   @schedule = schedules(:one)
  # end

  # test "should get index" do
  #   sign_in @user
  #   get schedules_url
  #   assert_response :success
  # end

  # test "should get new" do
  #   sign_in @user
  #   get new_schedule_url
  #   assert_response :success
  # end

  # test "should create schedule" do
  #   assert_difference('Schedule.count') do
  #     post schedules_url, params: { schedule: { available: @schedule.available, date: @schedule.date, endTime: @schedule.endTime, position_id: @schedule.position_id, startTime: @schedule.startTime, user_id: @schedule.user_id } }
  #   end

  #   assert_redirected_to schedule_url(Schedule.last)
  # end

  # test "should show schedule" do
  #   sign_in @user
  #   get schedule_url(@schedule)
  #   assert_response :success
  # end

  # test "should get edit" do
  #   sign_in @user
  #   get edit_schedule_url(@schedule)
  #   assert_response :success
  # end

  # test "should update schedule" do
  #   patch schedule_url(@schedule), params: { schedule: { available: @schedule.available, date: @schedule.date, endTime: @schedule.endTime, position_id: @schedule.position_id, startTime: @schedule.startTime, user_id: @schedule.user_id } }
  #   assert_redirected_to schedule_url(@schedule)
  # end

  # test "should destroy schedule" do
  #   assert_difference('Schedule.count', -1) do
  #     delete schedule_url(@schedule)
  #   end

  #   assert_redirected_to schedules_url
  # end
end
