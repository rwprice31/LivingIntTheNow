<%= form_for(position) do |f| %>
  <% if position.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(position.errors.count, "error") %> prohibited this position from being saved:</h2>

      <ul>
      <% position.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %>
    <%= f.text_field :name %>
  </div>

  <div class="field">
    <%= f.label :editSchedule %>
    <%= f.check_box :editSchedule %>
  </div>

  <div class="field">
    <%= f.label :acceptDenyShift %>
    <%= f.check_box :acceptDenyShift %>
  </div>

  <div class="field">
    <%= f.label :scheduleable %>
    <%= f.check_box :scheduleable %>
  </div>

  <div class="field">
    <%= f.label :store_id %>
    <%= f.text_field :store_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
